name = "100-musicos-worker"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

# Durable Objects
[[durable_objects.bindings]]
name = "GAME_ROOM"
class_name = "GameRoom"

# Migrations: registra las clases de Durable Objects
[[migrations]]
tag = "v1"
new_classes = ["GameRoom"]

# Variables de entorno (agrega las tuyas en el dashboard de Cloudflare)
# [vars]
# ALLOWED_ORIGIN = "https://tu-app.vercel.app"
